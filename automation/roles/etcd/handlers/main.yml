---
# <mmkolpakov_changes>
- name: Restart etcd service
  ansible.builtin.systemd:
    daemon_reload: true
    name: etcd
    state: restarted
    enabled: true
  listen: "restart etcd"
# </mmkolpakov_changes>
